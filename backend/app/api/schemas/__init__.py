"""
HTTP-facing request/response schemas.

One module per agent or pipeline phase.
These schemas handle serialisation concerns (file uploads, response envelopes)
and are intentionally separate from the internal agent schemas.
"""
